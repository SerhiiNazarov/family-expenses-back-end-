const { getGeneralValue } = require("./getGeneralValue");
const { getDataByDate } = require("./getDataByDate");
const { addNote } = require("./addNote");

module.exports = {
  getGeneralValue,
  getDataByDate,
  addNote,
};
